using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpriteScaleEffect : MonoBehaviour {

    public float spriteScale;
    public float scaleMultiplier = 0.15f;

    
    public GameObject spritename;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        
            
     //transform.localScale = new Vector3(Mathf.PingPong(Time.time, 1) + scaleMultiplier, Mathf.PingPong(Time.time, 1) + scaleMultiplier, 0);
         




    }
}
